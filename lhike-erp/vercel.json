{
  "buildCommand": "npm install --prefix server && npm install --prefix client && npm run prisma:generate --prefix server && npm run build --prefix server && node server/scripts/provision-db.mjs && npm run build --prefix client",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
