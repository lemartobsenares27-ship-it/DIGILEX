{
  "name": "lhike-erp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Root package for the Netlify Function wrapper (netlify/functions/api.mts) -- the server and client apps have their own package.json under server/ and client/.",
  "dependencies": {
    "@netlify/database": "^1.1.0",
    "serverless-http": "^4.0.0"
  },
  "devDependencies": {
    "@netlify/functions": "^5.3.0"
  }
}
